//crie uma função chamada contagem regressiva que receba um número.
//Use um loop while para exibir no console a contagem até 0.

function ContagemRegressiva(numero) {
    while (numero >= 0) {
        console.log(numero);
         numero--;
        
    }
    
}
ContagemRegressiva(5);
